package net.dutymod.client.hudcache.compat;

import net.dutymod.client.hudcache.compat.sodium.SodiumCompat;
import net.minecraft.client.Minecraft;

public class CompatHelper {
    //? <=1.20.4 {
    /*public static boolean isVignetteEnabled() {
        if (SodiumCompat.INSTALLED) {
            return SodiumCompat.isVignetteEnabled();
        }
        else {
            return Minecraft.useFancyGraphics();
        }
    }
    *///? }
}

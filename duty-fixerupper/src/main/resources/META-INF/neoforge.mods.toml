modLoader = "javafml"
loaderVersion = "[1,)"
license = "GNU LGPL 3.0"

[[mods]]
modId = "duty_fixerupper"
version = "${mod_version}"
displayName = "Duty: FixerUpper"
logoFile = "icon.png"
authors = "Duty"
credits = "Derived from ModernFix by embeddedt (LGPL-3.0) and Modernica by Reverie Projects (LGPL-3.0)."
description = '''
Duty does what it needs to do, perform its duty.

FixerUpper: startup time and general throughput. Trims datafixer work, makes
resource and registry loading lazy, and cuts the long tail of startup stalls.
'''

[[mixins]]
config = "duty_fixerupper.mixins.json"

# The loader source set's mixins get their own generated config. They target NeoForge's own classes
# -- capabilities, registry events, its model pipeline -- and have no Fabric equivalent, so keeping
# them separate is what lets a Fabric build ship the config above without this one.
[[mixins]]
config = "duty_fixerupper_neoforge.mixins.json"

[[dependencies.duty_fixerupper]]
modId = "neoforge"
type = "required"
versionRange = "[26.1,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.duty_fixerupper]]
modId = "minecraft"
type = "required"
versionRange = "[26.1.2,26.2)"
ordering = "NONE"
side = "BOTH"

[[dependencies.duty_fixerupper]]
modId = "modernfix"
type = "incompatible"
reason = "Duty: FixerUpper already contains ModernFix. Running both applies the same patches twice."
versionRange = "[0,)"
ordering = "NONE"
side = "BOTH"
